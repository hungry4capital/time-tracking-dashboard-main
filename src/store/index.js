import { createStore } from "vuex";
import data from "../data/data.json";

export default createStore({
  state: {
    data: data,
  },
  getters: {},
  mutations: {},
  actions: {},
  modules: {},
});
